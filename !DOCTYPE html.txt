<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Sorry Meenu ❤️</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    font-family: 'Segoe UI', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
}

.container {
    background: white;
    padding: 25px;
    border-radius: 18px;
    text-align: center;
    width: 90%;
    max-width: 420px;
    box-shadow: 0 15px 30px rgba(0,0,0,0.25);
}

h1 {
    color: #ff4d6d;
    margin-bottom: 10px;
}

.message {
    font-size: 16px;
    min-height: 70px;
}

.gallery {
    display: flex;
    justify-content: center;
    gap: 10px;
    margin: 15px 0;
}

.gallery img {
    width: 85px;
    height: 85px;
    border-radius: 12px;
    object-fit: cover;
    transition: transform 0.3s;
}

.gallery img:hover {
    transform: scale(1.15);
}

button {
    background: #ff4d6d;
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 25px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    background: #e63b5c;
}

#hiddenMessage {
    margin-top: 15px;
    font-size: 18px;
    color: #ff4d6d;
    font-weight: bold;
}

/* Floating hearts */
.heart {
    position: absolute;
    color: #ff4d6d;
    font-size: 20px;
    animation: floatUp 5s linear infinite;
}

@keyframes floatUp {
    0% {
        transform: translateY(100vh) scale(1);
        opacity: 1;
    }
    100% {
        transform: translateY(-10vh) scale(1.5);
        opacity: 0;
    }
}
</style>
</head>

<body>

<div class="container">
    <h1>I’m Sorry ❤️</h1>

    <p class="message" id="typing"></p>

    <div class="gallery">
        <img src="images/pic1.jpg">
        <img src="images/pic2.jpg">
        <img src="images/pic3.jpg">
    </div>

    <button onclick="showMessage()">Forgive Me 🥺</button>

    <p id="hiddenMessage"></p>
</div>

<!-- Optional Background Music -->
<audio id="bgMusic" loop>
    <source src="music.mp3" type="audio/mpeg">
</audio>

<script>
/* Typing effect */
const text = "Sorry Meenu, I didn’t mean to hurt you.\nI just need you to be happy forever.\nSmile babyyy 😊";
let index = 0;
const speed = 50;

function typeEffect() {
    if (index < text.length) {
        document.getElementById("typing").innerHTML += 
            text.charAt(index) === "\n" ? "<br>" : text.charAt(index);
        index++;
        setTimeout(typeEffect, speed);
    }
}
typeEffect();

/* Button message */
function showMessage() {
    document.getElementById("hiddenMessage").innerText =
        "I promise I won’t do this anymore. Forgive me once 💖";
    document.getElementById("bgMusic").play();
}

/* Floating hearts */
setInterval(() => {
    const heart = document.createElement("div");
    heart.className = "heart";
    heart.innerText = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.fontSize = (Math.random() * 20 + 15) + "px";
    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 5000);
}, 500);
</script>

</body>
</html>
